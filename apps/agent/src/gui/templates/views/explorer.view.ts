// Modal de explorador web previo desactivado a petición explícita del usuario
// en favor del selector nativo de Windows (OpenFileDialog).
export function renderExplorerModal(): string {
  return '';
}
